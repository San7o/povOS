megs: 128
cpu: model=corei7_haswell_4770, count=1, ips=50000000, reset_on_triple_fault=1

# Path to the BIOS and VGA BIOS
romimage: file="/usr/share/bochs/BIOS-bochs-latest"
vgaromimage: file="/usr/share/bochs/VGABIOS-lgpl-latest"

# We use floppy now since the kernel is so small that bochs does not
# recognise it as a disk
# floppya: 1_44="povos", status=inserted
# boot: floppy

# Use hard disk, required the image to be at least 10M big
ata0: enabled=1, ioaddr1=0x1f0, ioaddr2=0x3f0, irq=14
ata0-master: type=disk, path="povos", mode=flat
boot: disk

# Uart
com1: enabled=1, mode=term, dev=/dev/stdout

# Log file for debugging
log: bochsout.txt

clock: sync=realtime
